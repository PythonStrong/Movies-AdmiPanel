import { Actor } from '@/public'
import Image from 'next/image'
import React from 'react'

export default function ActorModal({item, index,}) {
    return (
        <div className='w-full'>
            
        </div>
    )
}
